<nav class="navbar navbar-dark bg-primary navbar-expand-lg py-3">
    <a class="navbar-brand" href="{{ path('home') }}"><strong class="text-warning">NBA</strong> Stats</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item {{ app.request.get('_route') == 'players' ? 'active' }}">
                <a class="nav-link" href="{{ path('players') }}">Joueurs</a>
            </li>
            <li class="nav-item {{ app.request.get('_route') == 'compare' ? 'active' }}">
                <a class="nav-link" href="{{ path('compare') }}">Comparer</a>
            </li>
        </ul>
    </div>
</nav>